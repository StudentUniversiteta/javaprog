В приведенном примере функция getFilesInDirectory
    принимает путь к каталогу в виде строки и возвращает список файлов в этом каталоге.
    Она использует класс File, чтобы проверить существование каталога,
    перечислить файлы в каталоге и добавить их имена в список.

Функция printFirstFiveElements принимает список и печатает
первые пять элементов списка, используя цикл for. Это достигается
путем подсчета количества выведенных элементов с помощью переменной count
и прерывания цикла после вывода пятого элемента с помощью оператора break.

В функции main пример демонстрирует использование этих функций.
Он передает путь к каталогу в качестве аргумента для функции getFilesInDirectory,
сохраняет список файлов и передает его в функцию printFirstFiveElements,
чтобы вывести первые пять имен файлов на экран.